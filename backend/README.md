# Backend (NestJS) - FluentWork

This folder contains a minimal NestJS scaffold for FluentWork.

Quick start (locally):

```bash
cd backend
npm install
# set environment variables from .env.example
npm run start:dev
```

The app exposes a base server at `http://localhost:3000/api`.
